# homebrew-justty

Homebrew tap for [Justty](https://github.com/0x96f/justty).

```bash
brew install --cask 0x96f/justty/justty
```

Release tags on `0x96f/justty` bump this cask automatically when `HOMEBREW_TAP_TOKEN` is set.
